# Smart Layered Context - Cursor IDE Rules
# Автоматическая интеграция СЛК системы с Cursor IDE

## 🧠 АВТОЗАГРУЗКА КОНТЕКСТА
# При открытии этого проекта автоматически загружается Smart Layered Context система
@.context/manifest.json

## 🎯 ПОВЕДЕНИЕ AI ПОМОЩНИКА
Когда пользователь использует команды типа "слк [команда]":

### 🔤 ЯЗЫКОВЫЕ АЛИАСЫ (Русский ↔ English):
- "слк статус" = "slc status" 
- "слк валидация" = "slc validate"
- "слк оптимизация" = "slc optimize"
- "слк организация" = "slc organize"
- "слк очистка" = "slc cleanup"
- "слк мониторинг" = "slc monitor"
- "слк помощь" = "slc help"
- "слк шаблоны" = "slc templates"
- "слк поиск" = "slc search"
- "слк создать" = "slc create"
- "слк информация" = "slc info"
- "слк обнови" = "slc update-context"
- "слк перезагрузи" = "slc reload-context"
- "слк анализ" = "slc analyze-context"
- "слк рефлексия" = "slc reflection"
- "слк список" = "slc list"
- "слк экспорт" = "slc export"
- "слк импорт" = "slc import"
- "слк эволюция-валидация" = "slc evolution-validate"
- "слк архив" = "slc create-archive"
- "слк экспорт-ридми" = "slc export-readme"

### 📋 ОСНОВНЫЕ КОМАНДЫ:
1. ВСЕГДА используй CLI скрипт для загрузки контекста
2. Для "слк старт" - выполни: ./slc load-context "базовый старт"
3. Для "слк контекст [задача]" - выполни: ./slc load-context "[задача]"
4. Для "слк список" / "slc list" - выполни: ./slc list (показывает активные задачи с JSON выводом для AI)
5. Для "слк шаблоны" / "slc templates" - выполни: ./slc templates (показывает все шаблоны)
6. Для "слк найди [что]" / "slc search" - выполни: ./slc search "[что]"
7. Для "слк подумай [задача]" - выполни: ./slc intelligent-recommend "[задача]"
8. Для "слк создай [шаблон] [имя]" / "slc create" - выполни: ./slc create "[шаблон]" "[имя]"
9. Для "слк организуй" / "slc organize" - выполни: ./slc organize --dry-run (анализ) или ./slc organize (применить) - с JSON выводом для AI
10. Для "слк обнови" / "slc update-context" - выполни: ./slc update-context (с JSON выводом для AI)
11. Для "слк помощь" / "slc help" - выполни: ./slc help (показывает все команды)
12. Для "слк помощь [команда]" - выполни: ./slc help [команда] (помощь по конкретной команде)
13. Для "слк статус" / "slc status" - выполни: ./slc status (с JSON выводом для AI)
14. Для "слк очистка" / "slc cleanup" - выполни: ./slc cleanup --dry-run
15. Для "слк анализ" / "slc analyze-context" - выполни: ./slc analyze-context (с JSON выводом для AI)
16. Для "слк валидация" / "slc validate" - выполни: ./slc validate (с JSON выводом для AI)
17. Для "слк оптимизация" / "slc optimize" - выполни: ./slc optimize
18. Для "слк рефлексия" / "slc reflection" - выполни: ./slc reflection --verbose (режим рефлексии)
19. Для "слк перезагрузи" / "slc reload-context" - выполни: ./slc reload-context
20. Для "слк экспорт" / "slc export" - выполни: ./slc export (экспорт изменений в .slc-update)
21. Для "слк импорт" / "slc import" - выполни: ./slc import [пакет] (импорт .slc-update пакета)
22. Для "слк эволюция-валидация" / "slc evolution-validate" - выполни: ./slc evolution-validate [пакет] (валидация пакета)
23. Для "слк архив" / "slc create-archive" - выполни: ./slc create-archive (создание архива для развертывания)
24. Для "слк экспорт-ридми" / "slc export-readme" - выполни: ./slc export-readme [опции] (экспорт README для Redmine)
25. Отвечай на русском языке в стиле СЛК системы
26. НЕ загружай файлы вручную - всегда используй CLI автоматизацию

### 🎯 JSON КОНТЕКСТ ДЛЯ AI ИНТЕГРАЦИИ:
Команды с автоматическим JSON выводом для загрузки в AI контекст:
- ./slc update-context - обновление контекста с рекомендациями
- ./slc list - активные задачи с анализом фокуса 
- ./slc status - состояние системы с рекомендациями
- ./slc validate - проверка целостности с auto-fix предложениями
- ./slc organize - организация файлов с результатами
- ./slc analyze-context - анализ контекста с улучшениями

Эти команды автоматически выводят JSON с:
- ai_recommendations: умные рекомендации следующих действий
- next_commands: предлагаемые команды для продолжения
- suggested_files_to_load: файлы для загрузки в AI контекст
- status/результаты выполнения команды

## 🚀 АВТОМАТИЧЕСКИЕ ДЕЙСТВИЯ
При работе с этим проектом:

1. Всегда предлагай использовать СЛК шаблоны для новых задач
2. При вопросах о структуре - выполни "слк список" (задачи) или "слк шаблоны" (шаблоны)
3. При вопросах о задачах - выполни "слк список"
4. При поиске шаблонов - выполни "слк шаблоны" или "слк найди"
5. Для AI/ML задач - выполни "слк контекст машинное обучение"
6. Для веб-разработки - выполни "слк контекст веб-разработка"
7. Отвечай в контексте СЛК системы
8. ВАЖНО: Используй CLI автоматизацию вместо ручной загрузки файлов

## 💡 ПРИМЕРЫ ИНТЕГРАЦИИ
Пользователь: "Хочу создать чат-бота"
Ответ: "СЛК поможет! Выполняю: слк контекст AI агенты" → ./slc load-context "AI агенты"

Пользователь: "Нужна документация"  
Ответ: "Загружаю контекст документации..." → ./slc load-context "документация"

Пользователь: "слк список"
Ответ: → Автоматически выполняет: ./slc list (показывает активные задачи)

Пользователь: "слк шаблоны"  
Ответ: → Автоматически выполняет: ./slc templates (показывает все доступные шаблоны)

Пользователь: "слк помощь"
Ответ: → Автоматически выполняет: ./slc help (показывает все команды по категориям)

---
# Этот файл обеспечивает автоматическую интеграцию СЛК системы с Cursor IDE
# При открытии проекта AI помощник автоматически понимает контекст и доступные команды через манифест 

# Базовый экспорт в Textile
./slc export-readme

# Экспорт в Markdown с оглавлением и информацией о проекте  
./slc export-readme --format markdown --include-toc --project-info

# Русский алиас
слк экспорт-ридми --format textile --output redmine_wiki.textile

# С JSON контекстом для AI
./slc export-readme --json-context --format markdown 