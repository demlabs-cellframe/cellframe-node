# Common: Wait for Network Sync
# Shared component for waiting until network is synced

setup:
  - wait: 15s
    description: "Wait for network to sync"
  
  - cli: net -net stagenet get status
    node: node1
    expect: success
    save: sync_status
    description: "Check sync status"

