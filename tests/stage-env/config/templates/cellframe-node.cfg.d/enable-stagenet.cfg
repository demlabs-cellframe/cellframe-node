# Enable stagenet for testing
# This configuration enables only the stagenet

[stagenet]
enabled=true

# Role-specific configurations for master nodes
[role-master]
# Chains that master nodes should process
proc_chains=[0x0000000000000003]  # Process main chain (ESBocs consensus)

[role-full]
# Full nodes sync all data but don't participate in consensus