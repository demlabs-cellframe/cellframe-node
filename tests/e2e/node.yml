# E2E Test Suite: Node Operations
# Complete node information and management testing

suite: "Node Operations Suite"  # Suite name (this field marks it as suite descriptor)
description: End-to-end testing of node info, connections, and management
tags: [e2e, node, management]
version: "1.0"

network:
  topology: default

# This is a suite descriptor file (metadata only, no executable steps)
# Individual test scenarios are in the node/ subdirectory
