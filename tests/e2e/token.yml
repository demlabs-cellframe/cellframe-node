# E2E Test Suite: Token Operations
# Complete token lifecycle testing
suite: "Token Operations Suite"  # Suite name (this field marks it as suite descriptor)
description: End-to-end testing of token creation, emission, transfers, and updates
tags: [e2e, token, economics]

# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/set_net_default.yml
  - common/create_test_cert.yml

