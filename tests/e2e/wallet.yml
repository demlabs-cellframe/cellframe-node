# E2E Test Suite: Wallet Operations
# Complete wallet lifecycle testing

suite: "Wallet Operations Suite"  # Suite name (this field marks it as suite descriptor)
description: End-to-end testing of wallet creation, management, and operations
tags: [e2e, wallet, critical]
version: "1.0"

network:

# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/create_test_cert.yml
  - common/set_net_default.yml
  topology: default

# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/create_test_cert.yml
  - common/set_net_default.yml


# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/create_test_cert.yml
  - common/set_net_default.yml
# This is a suite descriptor file (metadata only, no executable steps)
# Individual test scenarios are in the wallet/ subdirectory
