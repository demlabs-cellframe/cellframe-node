# E2E Test Suite: Integration Tests
# Complex multi-component integration testing

suite: "Integration Tests Suite"
description: Complex integration tests covering multiple components and nodes
tags: [e2e, integration, complex]
version: "1.0"

network:
  topology: default

# This is a suite descriptor file (metadata only, no executable steps)
# Individual test scenarios are in the integration/ subdirectory
