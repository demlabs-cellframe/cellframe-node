# E2E Test Suite: Network Operations
# Complete network connectivity and management testing

suite: "Network Operations Suite"  # Suite name (this field marks it as suite descriptor)
description: End-to-end testing of network status, sync, and connectivity
tags: [e2e, network, connectivity]
version: "1.0"

network:
  topology: default

# This is a suite descriptor file (metadata only, no executable steps)
# Individual test scenarios are in the net/ subdirectory
