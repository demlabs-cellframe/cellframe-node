# E2E Test Suite: Chain Operations
# Complete blockchain operations testing

suite: "Chain Operations Suite"
description: End-to-end testing of chain list, info, and atom operations
tags: [e2e, chain, blockchain]
version: "1.0"

network:
  topology: default

# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/create_test_cert.yml
  - common/set_net_default.yml

