# UTXO Blocking Suite
# Tests for UTXO blocking functionality including arbitrage transactions

suite: "UTXO Blocking Suite"
description: "Functional tests for UTXO blocking and arbitrage transactions"
author: "Cellframe Team"
tags: [functional, utxo, blocking, arbitrage]
version: "1.0"

# Suite-level setup: executed once before all scenarios in this suite
includes:
  - common/create_test_cert.yml
  - common/set_net_default.yml

scenarios:
  - arbitrage_bypass_test.yml
  - tx_create_arbitrage.yml
