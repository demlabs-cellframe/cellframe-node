name: Node Connections Test
description: Verify node connections command returns uplinks/downlinks info
version: "1.0"

includes:
  - common/defaults.yml

test:
  - group: List connections on root node
    steps:
      - cli: "node connections"
        expect: success
        contains: "uplinks"
