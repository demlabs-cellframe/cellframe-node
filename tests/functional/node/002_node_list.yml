name: Node List Test
description: Verify node list returns the network nodes
version: "1.0"

includes:
  - common/defaults.yml

test:
  - group: List all nodes
    steps:
      - cli: "node list"
        expect: success
        contains: "addr"
