name: Node Balancer Test
description: Verify node balancer returns node addresses
version: "1.0"

includes:
  - common/defaults.yml

test:
  - group: Get balancer info
    steps:
      - cli: "node balancer"
        expect: success
        contains: "node_addr"
