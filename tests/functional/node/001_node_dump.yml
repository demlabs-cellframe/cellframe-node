name: Node Dump Test
description: Verify node dump returns actual node information
version: "1.0"

includes:
  - common/defaults.yml

test:
  - group: Dump node info
    steps:
      - cli: "node dump"
        expect: success
        contains: "addr"
