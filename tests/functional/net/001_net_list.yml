name: Network List Test
description: Verify network enumeration returns our network
version: "1.0"

includes:
  - common/defaults.yml

test:
  - group: List networks contains our network
    steps:
      - cli: "net list"
        expect: success
        contains: "{{network_name}}"
