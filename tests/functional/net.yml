# Functional Test Suite: Network Features
# Functional testing of advanced network features

suite: "Network Features Suite"
description: Functional testing of network sync, CA management, and advanced features
tags: [functional, network, features]
version: "1.0"

network:
  topology: default

# This is a suite descriptor file (metadata only, no executable steps)
# Individual test scenarios are in the net/ subdirectory
