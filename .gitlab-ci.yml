stages:
    - prepare
    - build
    - test
    - deploy
    - update

variables:
    GIT_SUBMODULE_STRATEGY: normal


cellframe-node_amd64:
    stage: build
    only:
     - ci-polygon
    
    tags:
     - ci-polygon
    script: ./build.sh

cellframe-node_arm64-amrhf:
    stage: build
    only:
     - ci-polygon
    tags:
     - ci-polygon
    script: ./build.sh
